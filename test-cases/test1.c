int main()
{

        // int b -= a;  
        // int b += a;

        // int b *= a;  
        // int b /= a;

        // int c %= a;
  
        // int x = sizeof(int);
       
        // int c = sizeof(unsigned int);


        // if (!a){
        //         break;
        //         return 0;
        // }
        // p = &a;

        a = (a + b) * 2;
        b = b + (a - 3) / 12;
}