class Tree:
        def __init__(self):
                self.tree = []
                self.root = None
                self.children = None
        def add_root(self, root):
                self.tree.append(root)
                self.root = root

        def traverse(self, )
        